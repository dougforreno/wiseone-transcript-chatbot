# Required API Keys (set in Streamlit Cloud Secrets or local .env)
# These are used for embeddings, chat, and vector database

# OpenAI - used for embeddings
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic - used for chat (Claude models)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Supabase - vector database for transcript storage
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Note: Model settings (CHAT_MODEL, TEMPERATURE, etc.) are now in config.py
# Edit config.py to tune the chatbot's behavior
